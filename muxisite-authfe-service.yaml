apiVersion: v1
kind: Service
metadata:
  namespace: muxisite
  labels:
    run: muxisite-auth-fe
  name: muxisite-auth-fe
spec:
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    run: muxisite-auth-fe
